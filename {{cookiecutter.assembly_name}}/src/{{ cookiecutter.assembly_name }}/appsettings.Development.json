{
  "Azure": {
    "KeyVault": {
      "VaultName": "{{cookiecutter.azure_key_vault_staging}}"
    },
    "Storage": {
      "Data": {
        "ConnectionString": "{{cookiecutter.azure_storage_connection_staging}}"
      },
      "ContainerName": "{{cookiecutter.azure_container_dev}}"
    }
  },
  "OAuth": {
    "Domain": "{{cookiecutter.oauth_domain_dev}}",
    "API": {
      "Audience": "{{cookiecutter.oauth_api_audience_dev}}"
    }
  }
}
